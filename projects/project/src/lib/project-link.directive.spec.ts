import { ProjectLinkDirective } from './project-link.directive';

describe('ProjectLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectLinkDirective();
    expect(directive).toBeTruthy();
  });
});
